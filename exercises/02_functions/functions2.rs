// functions2.rs
//
// 執行 `rustlings hint functions2` 或使用 `hint` watch 子命令來獲取提示。

// I AM NOT DONE

fn main() {
    call_me(3);
}

fn call_me(num:) {
    for i in 0..num {
        println!("Ring! Call number {}", i + 1);
    }
}
