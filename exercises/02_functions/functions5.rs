// functions5.rs
//
// 執行 `rustlings hint functions5` 或使用 `hint` watch 子命令來獲取提示。

// I AM NOT DONE

fn main() {
    let answer = square(3);
    println!("3 的平方是 {}", answer);
}

fn square(num: i32) -> i32 {
    num * num;
}
